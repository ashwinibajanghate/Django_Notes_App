import React from "react";

const Header = () => {
  return (
    <div
      className="app-header"
      style={{
        padding: "20px",
        backgroundColor: "#222",
        color: "#fff",
        textAlign: "center"
      }}
    >
      <h1>Notes App</h1>
    </div>
  );
};

export default Header;
